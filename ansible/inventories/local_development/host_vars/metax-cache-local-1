redis_instance:
  number: 1
  port: 6379
  ip: "{{ server_ip }}"
  is_master_node: true
  bind_ip_addresses: "{{ server_ip }}"

redis_sentinel_instance:
  number: 1
  port: 16379
  ip: "{{ server_ip }}"
  bind_ip_addresses: "{{ server_ip }}"
