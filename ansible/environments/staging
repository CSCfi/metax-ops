[dbservers]
# Use the below one for deployment
metax-db-test ansible_host="{{ primary_dbserver_external_ip }}" ansible_connection=ssh ansible_user=metax-user
# Use the below one for provisioning. Always do provisioning from a local environment.
#metax-db-test ansible_host="{{ primary_dbserver_external_ip }}" ansible_connection=ssh ansible_user=cloud-user ansible_ssh_private_key_file=../metax-cloud-user.pem

[webservers]
# Use the below one for deployment
metax-web-test ansible_host="{{ primary_webserver_external_ip }}" ansible_connection=ssh ansible_user=metax-user
# Use the below one for provisioning. Always do provisioning from a local environment.
#metax-web-test ansible_host="{{ primary_webserver_external_ip }}" ansible_connection=ssh ansible_user=cloud-user ansible_ssh_private_key_file=../metax-cloud-user.pem

[dataservers]
metax-data-test ansible_host="{{ primary_dataserver_external_ip }}" ansible_connection=ssh ansible_user=cloud-user ansible_ssh_private_key_file=../metax-cloud-user.pem

[staging:children]
dbservers
webservers
dataservers

[staging:vars]
deployment_environment_id=staging
primary_dbserver_external_ip=86.50.169.34
primary_webserver_external_ip=193.166.24.98
primary_webserver_internal_ip=192.168.1.11
primary_dataserver_external_ip=193.166.25.31
primary_dataserver_internal_ip=192.168.1.8
