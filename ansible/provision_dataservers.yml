---

- hosts: dataservers
  vars:
    is_provision: true
  become: yes
  roles:
    - os-base
    - python
    - rabbitmq
    - elasticsearch
  handlers:
    - name: restart sshd
      service: name=sshd state=restarted
