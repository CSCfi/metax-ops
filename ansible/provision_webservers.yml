---

- hosts: cacheservers
  vars:
    is_provision: true
  become: yes
  roles:
    - os-base
    - python
    - nginx
    - provision_django
  handlers:
    - name: restart sshd
      service: name=sshd state=restarted
