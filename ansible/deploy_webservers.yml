---

- hosts: webservers
  vars:
    is_provision: false
  become: yes
  roles:
    - update_packages
    - deploy_webservers
    - { role: reboot, when: deployment_environment_id not in ['local_development', staging', 'production']}
